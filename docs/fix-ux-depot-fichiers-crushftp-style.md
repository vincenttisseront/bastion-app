# Fix UX — fichiers façon CrushFTP

Navigateur par dossiers, drag & drop zéro-formulaire, auto-version par (folder_id, label),
droits hérités via AccessGrant(resource_type=folder). SemVer strict abandonné.
