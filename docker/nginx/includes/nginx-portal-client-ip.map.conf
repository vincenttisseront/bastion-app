# Prefer edge X-Real-IP (reverse01) over the docker peer ($remote_addr = Traefik).
# Without this, every session records 172.24.0.x instead of the browser client.
map $http_x_real_ip $portal_client_real_ip {
    ""      $remote_addr;
    default $http_x_real_ip;
}
