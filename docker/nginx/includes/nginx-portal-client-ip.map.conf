# Prefer edge X-Real-IP (reverse01) when present; else $remote_addr
# (after real_ip module, $remote_addr is the original client from XFF).
map $http_x_real_ip $portal_client_real_ip {
    ""      $remote_addr;
    default $http_x_real_ip;
}
