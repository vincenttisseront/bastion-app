# Legacy maps from PR #21 (parent-capture ↔ native fallback).
# Snippets now use $host / $http_cookie / $cookie_bastion_session directly
# (portal_auth_check parity). Kept as a no-op include so older images /
# docs that reference this file still nginx -t cleanly.
map $remote_addr $bastion_subdomain_auth_maps_loaded {
    default 1;
}
