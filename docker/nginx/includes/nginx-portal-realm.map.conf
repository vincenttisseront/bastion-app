# Prefer realm from auth_request (native bastion_session / oauth2) over legacy cookie.
# $auth_realm is set via auth_request_set in portal locations; empty → cookie fallback.
map $auth_realm $portal_x_realm_slug {
    default $auth_realm;
    ""      $cookie_portal_realm_slug;
}
