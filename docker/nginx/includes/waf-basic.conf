# Bastion ModSecurity custom exclusions (included from main-*.conf AFTER CRS).
# This file is ModSecurity syntax only — do NOT nginx-include it (breaks nginx -t).
#
# Cutover 2026-08-06: empty until a confirmed false positive needs a targeted rule.
# Examples:
#   SecRuleRemoveById 942100
#   SecRuleUpdateTargetById 942100 "!ARGS:password"
