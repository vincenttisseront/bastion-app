# Default engine mode overlay.
# EMERGENCY 2026-08-06: not Included by main-*.conf (DB profile « on » was forcing
# SecRuleEngine On after static engine-*.conf). Re-Include only after ModSec 500 is fixed.
SecRuleEngine Off
