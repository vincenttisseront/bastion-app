# Bastion ModSecurity core — engine mode lives in engine-*.conf (per-family toggle).
# Adapted from ModSecurity v3 recommended + OWASP CRS docker defaults.

SecRequestBodyAccess On
SecRequestBodyLimit 13107200
SecRequestBodyNoFilesLimit 131072
SecRequestBodyLimitAction Reject
SecRequestBodyJsonDepthLimit 512
SecArgumentsLimit 1000
SecPcreMatchLimit 100000
SecPcreMatchLimitRecursion 100000

# Never log response bodies (vault credentials, session cookies, app HTML).
SecResponseBodyAccess Off

SecTmpDir /tmp/modsecurity/tmp/
SecDataDir /tmp/modsecurity/data/
SecUploadDir /tmp/modsecurity/upload/
SecUploadFileMode 0600
SecUploadKeepFiles Off
SecTmpSaveUploadedFiles Off

SecAuditEngine Off
# Audit re-enable after ModSec request-time 500 is resolved (was RelevantOnly →
# /var/log/nginx/apps/modsec_audit.log). Unwritable audit path can itself 500.
# SecAuditEngine RelevantOnly
# SecAuditLogRelevantStatus "^(?:5|4(?!04))"
# SecAuditLogParts ABIJDEFHZ
# SecAuditLogType Serial
# SecAuditLogFormat JSON
# SecAuditLog /var/log/nginx/apps/modsec_audit.log
SecAuditLogRelevantStatus "^(?:5|4(?!04))"
SecAuditLogParts ABIJDEFHZ
SecAuditLogType Serial
SecAuditLogFormat JSON
SecAuditLog /dev/null

SecArgumentSeparator &
SecCookieFormat 0
SecUnicodeMapFile /etc/modsecurity.d/unicode.mapping 20127
SecStatusEngine Off
SecDebugLog /dev/null
SecDebugLogLevel 0

SecRule REQUEST_HEADERS:Content-Type "^(?:application(?:/soap\+|/)|text/)xml" \
    "id:200000,phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=XML"
SecRule REQUEST_HEADERS:Content-Type "^application/json" \
    "id:200001,phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"
SecRule REQUEST_HEADERS:Content-Type "^application/[a-z0-9.-]+[+]json" \
    "id:200006,phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"
SecRule REQBODY_ERROR "!@eq 0" \
    "id:200002,phase:2,t:none,log,deny,status:400,msg:'Failed to parse request body.',logdata:'%{reqbody_error_msg}',severity:2"
