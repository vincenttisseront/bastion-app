# Ansible managed — source: awx-playbook/roles/nginx_reverse_proxy_dmz/files/transfer-crushftp.map.conf
# CrushFTP transfer — cookies vers backend (bloc http)
#
# Toujours $http_cookie : le mode "safe" (CrushAuth + 1 currentAuth* si _kc_*)
# cassait la session new-ui (boucle login). Login natif sur ce vhost.

map $http_cookie $transfer_crushftp_backend_cookie {
    default $http_cookie;
}
