# Géré par Ansible (rôle sso_portal / Phase 6) — http context
# Cookie break-glass (bg_session) → header auth_source (validation JWT côté FastAPI)
map $cookie_bg_session $portal_x_auth_source {
    default "oauth2";
    "~.+"  "break-glass";
}
