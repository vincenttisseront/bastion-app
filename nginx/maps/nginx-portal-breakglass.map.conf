# Géré par Ansible (rôle sso_portal / Phase 6) — http context
# Cookie break-glass présent → header auth_source (validation JWT côté FastAPI)
map $cookie_portal_breakglass_token $portal_x_auth_source {
    default "oauth2";
    "~.+"  "break-glass";
}
